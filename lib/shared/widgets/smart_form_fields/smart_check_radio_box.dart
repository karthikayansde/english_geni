// This file is commented out to prevent deprecated member warnings.
// To use, place this in the project and uncomment this code.
/*
import 'package:flutter/material.dart';
import 'package:reactive_forms/reactive_forms.dart';
import 'smart_form_field_theme.dart';

class SmartCheckRadioBox<T> extends StatelessWidget {
  final String formControlName;
  final String labelText;
  final Widget? prefixIcon;
  final Widget? suffixIcon;
  final bool isGreenWarnNeed;
  final Map<String, ValidationMessageFunction>? validationMessages;
  final void Function(dynamic value)? onChanged;
  final List<Map<T, String>> items;
  final bool isSingleSelect;
  final bool isWrap;

  const SmartCheckRadioBox({
    super.key,
    required this.formControlName,
    required this.labelText,
    this.prefixIcon,
    this.suffixIcon,
    this.isGreenWarnNeed = false,
    this.validationMessages,
    this.onChanged,
    required this.items,
    this.isSingleSelect = true,
    this.isWrap = false,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
*/